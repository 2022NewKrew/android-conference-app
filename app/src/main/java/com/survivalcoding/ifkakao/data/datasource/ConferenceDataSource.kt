package com.survivalcoding.ifkakao.data.datasource

import javax.inject.Inject

class ConferenceDataSource @Inject constructor(conferenceService: ConferenceService) {
    suspend fun getData() {
    }
}